import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  containerAddDetails: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
});